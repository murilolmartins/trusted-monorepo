import styled from 'styled-components';

export const HomeRegisterUsersContainer = styled.div`
  text-align: center;
  display: flex;
  flex-direction: column;
  gap: 10px;
`;

export const HomeRegisterUsersTitle = styled.h3`
  font-size: ${({ theme }) => theme.font.sizes.xhuge};
`;

export const HomeRegisterUsersDescription = styled.p``;
